<%= form_for(route) do |f| %>
    <%= render 'shared/errors_messages', resource: route %>

    <div class="field">
      <%= f.label :title %>
      <%= f.text_field :title %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
