<h1><%= t('.trains')%></h1>
<div class="row">
  <div class="col-md-6">
    <table class="table">
      <thead>
        <tr>
          <th><%= t('admin.trains.attributes.number') %></th>
          <th colspan="3" class="text-right"><%= t('common.actions') %></th>
        </tr>
      </thead>

      <tbody>
        <%= render @trains %>
      </tbody>
    </table>
  </div>
</div>

<br>

<%= link_to t('common.add'), new_admin_train_path %>
